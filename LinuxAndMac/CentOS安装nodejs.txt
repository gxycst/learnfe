1.tar zxvf node.version.tar.gz  //解压
2.vim /etc/profile   //设置环境变量

#set for nodejs
export NODE_HOME=/usr/local/node/
export PATH=$NODE_HOME/bin:$PATH

3.使配置生效
source /etc/profile

4.查看是否安装成功
node -v || npm -v